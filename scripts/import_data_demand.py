# load res4d for all runs (4 runs)
# concatenate residual timeseries across the 4 runs
# square residuals and calculate average (resMS)

# for CondAll:
# load beta (_pe_) for each regressor
# calculate average beta and divide by resMS
